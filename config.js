// Optional: paste your Google OAuth Web client ID so this page can connect
// without opening Settings. Client IDs are not secret; authorized origins
// in Google Cloud still control who can use it.
window.WEEKEND_CALENDAR_CLIENT_ID = "";
